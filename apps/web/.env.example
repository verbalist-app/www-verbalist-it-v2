# Data Source Toggle
# Set to "true" to use Sanity CMS, "false" (or omit) to use Content Collections
USE_SANITY=false

# Sanity configuration (only required when USE_SANITY=true)
# Get these from https://sanity.io/manage
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_VERSION=2024-01-01

# Optional: Required only for draft/preview mode
# Create a token at https://sanity.io/manage → API → Tokens
# SANITY_READ_TOKEN=

# Required for migration script (pnpm migrate)
# Create a token with Editor permissions at https://sanity.io/manage → API → Tokens
# SANITY_WRITE_TOKEN=
